# Deps and build output are produced inside the image, never copied in.
node_modules
.nuxt
.output
.data

# Cloudflare / wrangler local state (.dev.vars holds secrets -- must not ship)
.wrangler
.dev.vars
.dev.vars.*

# Local env
.env
.env.*

# VCS and tooling
.git
.gitignore
.claude

# Testing
coverage
test-results
playwright-report

# Docker / deploy scaffolding (not needed by the build itself)
Dockerfile
.dockerignore
deploy

# Misc
.DS_Store
*.log
